{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch a.html",
            "type": "chrome",
            "request": "launch",
            "file": "${workspaceRoot}/a.html",
            "sourceMaps": true,
            "webRoot": "${workspaceRoot}"
        }
    ]
}